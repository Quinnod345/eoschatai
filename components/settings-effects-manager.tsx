'use client';

/**
 * Previously managed data-glass-effects and data-eos-gradients HTML attributes.
 * Both features have been removed; this component is now a no-op kept for
 * backwards-compatible import compatibility.
 */
export function SettingsEffectsManager() {
  return null;
}
