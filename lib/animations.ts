// GSAP temporarily disabled for build
// import { gsap } from 'gsap';
// import { ScrollTrigger } from 'gsap/dist/ScrollTrigger';

// Register ScrollTrigger plugin - DISABLED FOR BUILD
// if (typeof window !== 'undefined') {
//   gsap.registerPlugin(ScrollTrigger);
// }

// Initialize animations that should run on page load
export const initAnimations = () => {
  // GSAP animations temporarily disabled for build
  console.log('GSAP animations disabled temporarily');
  return;
};

// Parallax effects for decorative elements - DISABLED FOR BUILD
const setupParallaxEffects = () => {
  // GSAP temporarily disabled for build
  return;
};

// Animate sections as they enter the viewport
const setupSectionAnimations = () => {
  // GSAP temporarily disabled for build
  return;
};

// Animate feature cards on scroll
const setupFeatureAnimations = () => {
  // GSAP temporarily disabled for build
  return;
};

// Animate testimonials on scroll
const setupTestimonialAnimations = () => {
  // GSAP temporarily disabled for build
  return;
};