'use client';

import { useEffect } from 'react';

export default function ClientChatAnimation() {
  useEffect(() => {
    // GSAP animations temporarily disabled for build
    console.log('ClientChatAnimation disabled temporarily');
  }, []);

  return null;
}